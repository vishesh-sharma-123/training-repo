import { RangePipePipe } from './range-pipe.pipe';

describe('RangePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RangePipePipe();
    expect(pipe).toBeTruthy();
  });
});
